# Arduino LED Dimmer Control (GUI)

Программа с графическим интерфейсом (GUI) на Python для управления светодиодом, подключенным к Arduino. Позволяет включать/выключать светодиод, регулировать его яркость с помощью ползунка и управлять через клавиатуру.

Проект использует библиотеку `tkinter` для интерфейса  и протокол **Firmata** для общения с платой Arduino.

## ✨ Возможности

**Плавная регулировка яркости**: Использование ШИМ (PWM) через виджет `Scale`.
* **Таймер задержки**: Настройка времени работы светодиода.
**Управление с клавиатуры**: Горячие клавиши для быстрого управления.


**Современный интерфейс**: Использование макета `Grid` для адаптивного расположения элементов и меню настроек.



## 🛠 Требования

### Аппаратное обеспечение

* Arduino Uno (или аналог).
* Светодиод (LED).
* Резистор (220 Ом).
* Соединительные провода.

### Программное обеспечение

* Python 3.x
* Библиотека `pyfirmata`

## ⚙️ Установка и запуск

1. **Установите необходимые библиотеки Python:**
```bash
pip install pyfirmata

```


2. **Подготовьте Arduino:**
* Откройте Arduino IDE.
* Перейдите в `Файл` -> `Примеры` -> `Firmata` -> `StandardFirmata`.
* Загрузите этот скетч в вашу плату Arduino.
* *Важно:* Без этого шага Python не сможет управлять платой!


3. **Схема подключения:**
* **Анод LED (+)** (длинная ножка) -> **Пин 3** (PWM) на Arduino.
* **Катод LED (-)** (короткая ножка) -> **GND** через резистор 220 Ом.


4. **Настройка порта:**
Откройте файл `main.py` и убедитесь, что указан верный COM-порт вашей Arduino (например, `COM3` для Windows или `/dev/ttyUSB0` для Linux):
```python
board = Arduino("COM3") 

```


5. **Запуск:**
```bash
python main.py

```



## 🎮 Управление

### Графический интерфейс

* **Поле ввода**: Укажите время задержки (в секундах).
* **Ползунок (Scale)**: Перетащите для изменения яркости от 0% до 100%.
* **Кнопки**: Нажмите "ВКЛ" или "ВЫКЛ" для управления светодиодом.

### Горячие клавиши (Keyboard Shortcuts)

Благодаря функции `bind`, вы можете управлять устройством с клавиатуры:

* **Клавиша `1**`: Включить светодиод (максимальная яркость).
* **Клавиша `0**`: Выключить светодиод.
* **`Ctrl + Q`**: Закрыть программу.
